<%= form_for(@cocktail, html: { multipart: true }) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :picture %>
  <%= f.file_field :picture %>
  <%= f.submit "Add a cocktail" %>
<% end %>
